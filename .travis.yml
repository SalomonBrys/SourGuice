language: java

notifications:
  webhooks:
    urls:
      - https://webhooks.gitter.im/e/e581add5f7f207b5248a
    on_success: always  # options: [always|never|change] default: change
    on_failure: always  # options: [always|never|change] default: always
    on_start: false     # default: false

  email:
    recipients:
      - salomon.brys@gmail.com
    on_success: change  # options: [always|never|change] default: change
    on_failure: always  # options: [always|never|change] default: always
    on_start: false     # default: false
